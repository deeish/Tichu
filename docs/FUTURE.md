******* double check the dragon frontend and how it looks*******


-Add fly.io so the backend can run in multiple regions(mostly for soom)

Might need to add:
-if multiple people leave have them pick which seat to sit at instead of auto join
-when server is loading usually a minute, should i make a loading screen?

************
-Add an example after combinations for quenchies

- fix scroll on card definition

-Fix flow steps

Fix phoenix wording for -25 points

Ask about this
-Flow step 1
“The leader plays any legal combination. For the opening trick.” reads like a fragment. You already say elsewhere that the Mah Jong holder must open with Mah Jong, so “any legal combination” for the opening trick is a bit at odds with that. Either tie it explicitly to “after the opening trick” or rephrase so it’s clearly about the leader in general, with opening covered above.

